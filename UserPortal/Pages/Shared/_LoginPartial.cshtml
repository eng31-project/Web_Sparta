@if (false)
{
    <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })" method="post" id="logoutForm" class="navbar-right">
        <ul class="nav navbar-nav navbar-right">
            <li>
                <a asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello dinges!</a>
            </li>
            <li>
                <button type="submit" class="btn btn-danger navbar-btn navbar-link">Logout</button>
            </li>
        </ul>
    </form>
}
else
{
    <ul class="nav navbar-nav navbar-right">
        @if (Url.ActionContext.HttpContext.Request.Path != "/")
        {
            <li><a class="btn btn-danger" asp-area="" asp-page="/Login">Logout</a></li>
        }
    </ul>
}
